<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="login">
  	<select id="login" parameterType="mvo">
  		select s.memnum, s.mempwd, s.memprofile, s.memname, s.memjob,s.memdept,s.memmgr, d.dename, m.memname mgrname, s.memauth,s.meminmail
		from member s, member m, dept d where s.memmgr = m.memnum and s.memdept = d.denum and s.memnum = #{memnum} and s.mempwd =#{mempwd}
  	</select>
  	
  </mapper>