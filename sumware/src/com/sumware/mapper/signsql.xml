<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sign">
	<!-- signForm 저장 하는 쿼리. -->
	<insert id="sfInsert" parameterType="sfvo">
	</insert>
	
	<!-- 해당부서의 결재 문서를 조회 하는 쿼리. model 에서 getSignList 와 맵핑 -->
	<select id="getSgList" parameterType="sgvo" resultType="sgvo">
		 
	</select>
	
	<!-- 회사내규에 있는 결재 문서들의 종류를 보여주는 메소드 -->
	<!-- 멤버의 정보로 결재문서들을 불러온다. -->
	<select id="getSfList" resultType="sfvo">
		select * from signform
	</select>
	<select id="getMgr" parameterType="int" resultType="mvo">
		select * from member where memnum=(select memmgr from member where memnum=#{memnum})
	</select>
	
	<!-- 결재문서의 종류 중 하나를 택해서 작성 한다. -->
	<select id="getSf" parameterType="sfvo" resultType="sfvo">
	</select>
	
	<!-- 선택된 문서를 작성 한다 -->
	<insert id="writeSf" parameterType="map">
	</insert>
	
	<!-- 결재된 문서를  -->
	
</mapper>